# LoL Pro Stats ETL — extraction from Leaguepedia (Cargo) and Oracle's Elixir.
# mwcleric fork recommended by Leaguepedia: retries on Cargo's new rate limits
# (see Help:Leaguepedia_API). Needs a bot-password login + a confirmed email.
mwcleric @ git+https://github.com/arbolitoloco1/mwcleric@empty_string_fix
mwrogue>=0.1.5
# Row-schema validation.
pydantic>=2.6
# HTTP for Oracle's Elixir (CSV downloads) and utilities.
httpx>=0.27
# Handling Oracle's Elixir's large CSVs (Phase 2).
polars>=0.20
</content>
